<%= form_for(@micropost2) do |f| %>
  <% if @micropost2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micropost2.errors.count, "error") %> prohibited this micropost2 from being saved:</h2>

      <ul>
      <% @micropost2.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
